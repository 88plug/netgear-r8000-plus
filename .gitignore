# large firmware binaries — kept on disk, not in git
images/*.chk
images/*.zip
*.bin

# OpenWRT source clone + build artifacts (huge)
openwrt/
imagebuilder*/
*.tar.zst
build/
bin/

# python / scratch
__pycache__/
*.pyc
extracted/nvram-stock.txt
v2-files/etc/config/wireless
v2-files/lib/modules/
images/packages/*.apk
