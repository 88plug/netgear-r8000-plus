# large firmware binaries — kept on disk, not in git
images/*.chk
images/**/*.chk
images/*.manifest
images/*.zip
*.bin

# OpenWRT source clone + build artifacts (huge)
openwrt/
openwrt-imagebuilder*/
openwrt-sdk*/
imagebuilder*/
*.tar.zst
build/
bin/

# backups (contain live device config, kept locally only)
backups/
images/backup-*.tar.gz

# vendor stock-firmware rootfs extraction (huge, third-party binaries)
hwoffload-research/blob-analysis/rootfs/
hwoffload-research/blob-analysis/extract/
*.tar

# ghidra-cli is its own separate tool/repo (has its own .git), not part of this project
hwoffload-research/ghidra-cli/

# large raw disassembly dumps (research artifacts, kept locally)
v2-staging/firmware/re-analysis/full_disasm.txt
v2-staging/firmware/re-analysis/strings_with_offsets.txt

# out-of-tree Kbuild artifacts (regeneratable via `make`; keep .c/Makefile/docs only)
hwoffload-research/fa-probe/*.ko
hwoffload-research/fa-probe/*.o
hwoffload-research/fa-probe/*.mod
hwoffload-research/fa-probe/*.mod.c
hwoffload-research/fa-probe/.*.cmd
hwoffload-research/fa-probe/Module.symvers
hwoffload-research/fa-probe/modules.order

# python / scratch
__pycache__/
*.pyc
extracted/nvram-stock.txt
v2-staging/maxpower/verify/nvram-backup-pretest.txt
v2-staging/maxpower/verify/final-state.txt
v2-files/etc/config/wireless
v2-files/lib/modules/
images/packages/*.apk

# agent-session context file (local only, never published)
/CLAUDE.md
